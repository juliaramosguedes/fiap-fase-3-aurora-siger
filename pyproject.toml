[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fiap-fase-3-aurora-siger"
version = "3.0.0"
requires-python = ">=3.13"
dependencies = []

[project.optional-dependencies]
dev = []
